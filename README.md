# Claude Skills

Web app and coding learning project.

Quick start:
- Install: npm install
- Run dev: npm run dev
- Start: npm start

API endpoints:
- GET /api/skills — list skills
- GET /api/skills/:id — get skill by id
